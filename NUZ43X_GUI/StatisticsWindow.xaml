<Window x:Class="NUZ43X_GUI.StatisticsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Statisztika"
        Height="650"
        Width="1000"
        WindowStartupLocation="CenterOwner"
        FontSize="14">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <GroupBox Header="Összesített adatok" Margin="0,0,0,15">
            <UniformGrid Columns="5" Margin="10">
                <StackPanel Margin="10">
                    <TextBlock Text="Edzések száma" FontWeight="Bold"/>
                    <TextBlock x:Name="WorkoutCountTextBlock" FontSize="18"/>
                </StackPanel>

                <StackPanel Margin="10">
                    <TextBlock Text="Sorozatok száma" FontWeight="Bold"/>
                    <TextBlock x:Name="TotalSetsTextBlock" FontSize="18"/>
                </StackPanel>

                <StackPanel Margin="10">
                    <TextBlock Text="Ismétlések száma" FontWeight="Bold"/>
                    <TextBlock x:Name="TotalRepetitionsTextBlock" FontSize="18"/>
                </StackPanel>

                <StackPanel Margin="10">
                    <TextBlock Text="Összvolumen" FontWeight="Bold"/>
                    <TextBlock x:Name="TotalVolumeTextBlock" FontSize="18"/>
                </StackPanel>

                <StackPanel Margin="10">
                    <TextBlock Text="Gyakorlatok száma" FontWeight="Bold"/>
                    <TextBlock x:Name="ExerciseCountTextBlock" FontSize="18"/>
                </StackPanel>
            </UniformGrid>
        </GroupBox>

        <TextBlock Grid.Row="1"
                   Text="Gyakorlatonkénti statisztika"
                   FontSize="18"
                   FontWeight="Bold"
                   Margin="0,0,0,10"/>

        <DataGrid x:Name="StatisticsDataGrid"
                  Grid.Row="2"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  SelectionUnit="FullRow">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Gyakorlat"
                                    Binding="{Binding ExerciseName}"
                                    Width="2*"/>
                <DataGridTextColumn Header="Alkalmak száma"
                                    Binding="{Binding WorkoutOccurrences}"
                                    Width="*"/>
                <DataGridTextColumn Header="Sorozatok száma"
                                    Binding="{Binding TotalSets}"
                                    Width="*"/>
                <DataGridTextColumn Header="Össz ismétlés"
                                    Binding="{Binding TotalRepetitions}"
                                    Width="*"/>
                <DataGridTextColumn Header="Max súly"
                                    Binding="{Binding MaxWeight, StringFormat=F2}"
                                    Width="*"/>
                <DataGridTextColumn Header="Összvolumen"
                                    Binding="{Binding TotalVolume, StringFormat=F2}"
                                    Width="*"/>
                <DataGridTextColumn Header="Legjobb becsült 1RM"
                                    Binding="{Binding BestEstimatedOneRm, StringFormat=F2}"
                                    Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>